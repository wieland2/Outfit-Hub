<%= link_to "Home", offers_path%>

<h1> <%= @offer.title %></h1>


<p>Title: <%= @offer.title %></p>
<p>Details: <%= @offer.description %></p>
<p>Price: <%= @offer.price %></p>
<p>Size: <%= @offer.size %></p>

<%= link_to "Delete this offer",
  offer_path(@offer),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this offer?"}
%>
<%= link_to "Edit", edit_offer_path(@offer) %>
